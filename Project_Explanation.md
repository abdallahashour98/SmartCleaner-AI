# الدليل الشامل لمشروع استوديو التبييض والتنظيف الذكي (SmartCleaner-AI)

مرحباً بك في **SmartCleaner-AI**، الأداة المتخصصة والمستقلة المصممة خصيصاً لمبيضي ومحرري المانجا والقصص المصورة والويب تون (Manga / Webtoon Cleaners & Redrawers).

تم تجريد هذا المشروع من كافة تعقيدات استخراج النصوص وملفات الترجمة ومطابقة الـ JSON، ليصبح تطبيقاً خفيفاً وسريعاً وفائق الدقة مخصصاً بنسبة 100% لعمليات **التبييض، إزالة النصوص والمؤثرات، وإعادة بناء الخلفيات بالذكاء الاصطناعي (AI Inpainting & Redrawing)**.

---

## 🚀 ميزات SmartCleaner-AI الأساسية:

1. **اكتشاف تلقائي ذكي لفقاعات الكلام (YOLOv8 + ComicTextDetector):**
   - تحديد فائق الدقة لحدود الفقاعات عبر نموذج الذكاء الاصطناعي المدرب على أكثر من 8,000 صفحة مانجا.
   - تمديد الحواف تلقائياً لتغطية ذيول الفقاعات ومحيط النصوص بالكامل.

2. **التنظيف التكيفي السريع (Smart Adaptive Inpainting):**
   - يكتشف البرنامج نوع خلفية كل فقاعة تلقائياً:
     - **الخلفيات البيضاء النقية (`flat_white`):** يتم تبييضها محلياً في زمن خيالي (<0.002 ثانية للفقاعة!).
     - **الفقاعات السوداء والداكنة المصمتة (`flat_dark`):** يتم تفريغ النصوص البيضاء منها وتعبئتها بالأسود الصافي `[0, 0, 0]` فورياً في أقل من 0.002 ثانية مع حماية إطارها الخارجي دون الحاجة لـ GPU.
     - **الخلفيات المعقدة والراستر (Screentone / Art):** يتم توجيهها تلقائياً لمحرك الذكاء الاصطناعي العميق IOPaint (`anime-lama` / `lama` / `manga`) لإعادة بنائها ببراعة.
   - يوفر سرعة معالجة مجمعة تصل إلى 10 ثوانٍ لفصل كامل!

3. **عصا التبييض السحرية (🪄 Magic Wand Tool):**
   - اضغط بنقرة واحدة داخل أي فقاعة على صفحة المانجا ليقوم البرنامج باكتشاف حدودها وتبييضها فورياً.

4. **أداة الفرشاة الحرة (🖌️ Brush Inpaint Mask Tool):**
   - رسم قناع حر بالفرشاة بحجم قابل للتعديل لتبييض المؤثرات الصوتية (SFX) أو النصوص الحرة خارج الفقاعات.

5. **المقارنة الحية بنقرة واحدة (✨ Live Toggle Cleaned / Original):**
   - اضغط على زر `Space` أو `Tab` أو زر المعاينة للتبديل الفوري بين الصفحة الأصلية والصفحة المبيضة للتأكد من جودة التبييض.

6. **استعادة الأصل والتراجع (↩️ Revert / Undo):**
   - إمكانية استعادة أي فقاعة أو منطقة لشكلها الأصلي بضغطة زر واحدة.

7. **دعم السيرفر السحابي (Google Colab GPU):**
   - إمكانية تشغيل المعالجة على سيرفر كولاب للاستفادة من كروت الشاشة السريعة عن بُعد.

8. **التصدير المجمع (Batch Cleaned Export):**
   - تصدير كافة الصفحات المنظفة دفعة واحدة بصيغ عالية الجودة (`PNG`, `JPG`, `WebP`) أو كأرشيف مضغوط جاهز للتحرير.

9. **نظام التحديث التلقائي والجزئي (Modular Auto-Updater):**
   - البرنامج متصل بنظام تحديثات سحابي خفيف جداً (< 500 كيلوبايت) يفحص وجود إصدارات جديدة عند بدء التشغيل أو بضغطة زر.
   - تنزيل وتطبيق الإصلاحات والميزات البرمجية الجديدة واستبدال الملفات تلقائياً مع عمل نسخة احتياطية دون الحاجة لإعادة تحميل أو إرسال المشروع والمكتبات الضخمة مرة أخرى.
   - أدوات متكاملة للمطور: `tools/create_update_patch.py` و `tools/apply_patch.py`.

10. **التحزيم الجاهز للمستخدم النهائي (End-User Portable Release):**
    - أداة متخصصة `tools/package_enduser_release.py` تقوم بتجميع نسخة محمولة نظيفة وخالية من ملفات التطوير في ثوانٍ، وجاهزة للإرسال للمستخدمين للتشغيل الفوري.

11. **التثبيت والتفعيل التلقائي لـ Ngrok والتحكم من الموبايل (1-Click Ngrok & Mobile Pairing):**
    - بمجرد إدخال سيريال / رمز Ngrok Auth Token والضغط على "⚡ تثبيت وتفعيل الخدمة فورياً":
      - يقوم التطبيق بالتحقق من وجود أداة Ngrok وتحميل النسخة الرسمية لنظام ويندوز تلقائياً إن لم تكن متوفرة.
      - يضبط السيريال ويشغل السيرفر والنفق في الخلفية.
      - يُنشئ رمز استجابة سريعة (QR Code) ورابط النفق العام لربط تطبيق الموبايل فورياً والتحكم بالتبييض والتنظيف عن بُعد.

---

## ⌨️ اختصارات لوحة المفاتيح الافتراضية:

| الوظيفة | الاختصار |
| :--- | :--- |
| **التبديل بين الأصل والتبييض** | `Space` / `Tab` |
| **عصا التبييض السحرية** | `W` |
| **أداة الفرشاة الحرة** | `B` |
| **إضافة مستطيل تبييض** | `A` |
| **تبييض الصفحة الحالية** | `C` |
| **استعادة الفقاعة للأصل** | `R` |
| **تكبير / تصغير** | `+` / `-` (أو عجلة الماوس) |
| **ملاءمة الشاشة** | `F` |
| **الصفحة التالية / السابقة** | `Right` / `Left` |
| **حفظ وتصدير الصورة** | `Ctrl + S` |

---

## 🛠️ متطلبات وطرق التشغيل:
- تشغيل التطبيق عبر `SmartCleaner.bat` أو `Run_SmartCleaner.bat` (أو `Run_SmartCleaner.vbs` للتشغيل الصامت).
- تشغيل سيرفر IOPaint المحلي على منفذ `8080` أو ربط سيرفر كولاب الخارجي.
- تشغيل خادم الموبايل والربط عن بُعد عبر نفق Ngrok بنقرة واحدة من داخل الواجهة.

